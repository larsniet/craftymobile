{
  "name": "crafty-relay",
  "version": "1.0.0",
  "description": "Zero-knowledge APNs relay for CraftyMobile, on Cloudflare Workers.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
